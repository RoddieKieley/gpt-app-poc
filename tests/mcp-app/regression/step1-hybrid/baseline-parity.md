# Baseline Parity Snapshot

- Step gating uses existing `navigateToStep` guards.
- Route/hash transitions remain `step-1`, `step-2`, `step-3`.
- Status semantics remain `info/success/warning/danger`.
- Tool call order and arguments remain unchanged in `src/mcp-app.ts`.
