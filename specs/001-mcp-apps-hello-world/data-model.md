# Data Model: MCP Apps Hello World

## Overview
This increment does not introduce persistent data entities. The Hello World response is generated at request time and returned as text and UI content.

## Entities
- None (no persistent storage or data lifecycle required).
